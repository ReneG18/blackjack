<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GameApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GameApp" parent="Base.Theme.GameApp" />

    <style name="Theme.GameApp.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- White background before layout loads -->
        <item name="android:windowBackground">@android:color/white</item>
        <!-- White status bar -->
        <item name="android:statusBarColor">@android:color/white</item>
    </style>

</resources>